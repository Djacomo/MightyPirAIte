name: ui-components-refactor
type: skill
version: 1.0.0
author: Djacomo

description: >
  Extracts a reusable UI component library and design tokens from existing HTML/CSS,
  then applies them consistently across the whole project — one incremental batch at a time.
  Built for backend and infrastructure engineers who need clean, maintainable frontend code
  without rewriting everything from scratch.

stack:
  - html-css
  - any

level:
  - mid
  - senior

claude-code-version: ">=1.0.0"
tested-on:
  - macOS
  - Linux

# Cost transparency
cost:
  level: free
  # This skill runs entirely inside Claude Code with no external service calls.
  # No accounts, no API keys, no network requests required.
  dependencies: []

tags:
  - frontend
  - refactor
  - design-system
  - components
  - css
